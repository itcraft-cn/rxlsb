# rxlsb IO层详细计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现 Bytes/BytesMut 零拷贝 IO 层，包括 BufferReader、BufferWriter、VarInt 编解码、UTF-16LE 处理

**Architecture:** bytes crate 零拷贝 + 小端序 + 流式读写

**Tech Stack:** bytes 1.5 / Rust std

---

## 文件结构

```
src/io/
├── mod.rs          # 模块入口
├── buffer_reader.rs # Bytes零拷贝读取
├── buffer_writer.rs # BytesMut零拷贝写入
├── varint.rs       # VarInt编解码
└── utf16.rs        # UTF-16LE处理
```

---

## Task 2.1: BufferReader

**Files:**
- Create: `src/io/buffer_reader.rs`
- Test: `tests/io/test_buffer_reader.rs`

- [ ] **Step 1: 创建测试文件 tests/io/test_buffer_reader.rs**

```rust
use rxlsb::io::BufferReader;
use bytes::Bytes;

#[test]
fn test_read_u32_le() {
    let data = Bytes::from_static(&[0x01, 0x00, 0x00, 0x00]);
    let mut reader = BufferReader::new(data);
    let value = reader.read_u32_le().unwrap();
    assert_eq!(value, 1);
}

#[test]
fn test_buffer_overflow() {
    let data = Bytes::from_static(&[0x01]);
    let mut reader = BufferReader::new(data);
    let result = reader.read_u32_le();
    assert!(result.is_err());
}
```

- [ ] **Step 2: 运行测试验证失败**

Run: `cargo test test_read_u32_le --no-run`
Expected: 编译错误（BufferReader未实现）

- [ ] **Step 3: 实现 src/io/buffer_reader.rs（核心结构和方法）**

```rust
use bytes::Bytes;
use crate::error::{XlsbError, Result};

pub struct BufferReader {
    buffer: Bytes,
    position: usize,
}

impl BufferReader {
    pub fn new(buffer: Bytes) -> Self {
        Self { buffer, position: 0 }
    }
    
    pub fn read_u32_le(&mut self) -> Result<u32> {
        if self.position + 4 > self.buffer.len() {
            return Err(XlsbError::BufferOverflow {
                position: self.position,
                length: self.buffer.len(),
            });
        }
        let bytes = &self.buffer[self.position..self.position + 4];
        let value = u32::from_le_bytes([bytes[0], bytes[1], bytes[2], bytes[3]]);
        self.position += 4;
        Ok(value)
    }
    
    pub fn read_u16_le(&mut self) -> Result<u16> { /* 类似实现 */ }
    pub fn read_u64_le(&mut self) -> Result<u64> { /* 类似实现 */ }
    pub fn read_f64_le(&mut self) -> Result<f64> { /* 类似实现 */ }
    pub fn read_u8(&mut self) -> Result<u8> { /* 类似实现 */ }
    
    pub fn read_bytes(&mut self, len: usize) -> Result<Bytes> {
        if self.position + len > self.buffer.len() {
            return Err(XlsbError::BufferOverflow { ... });
        }
        let slice = self.buffer.slice(self.position..self.position + len);
        self.position += len;
        Ok(slice)
    }
    
    pub fn position(&self) -> usize { self.position }
    pub fn remaining(&self) -> usize { self.buffer.len() - self.position }
    pub fn has_remaining(&self) -> bool { self.position < self.buffer.len() }
}
```

- [ ] **Step 4: 运行测试验证通过**

Run: `cargo test test_read_u32_le`
Expected: PASS

- [ ] **Step 5: 实现完整 BufferReader（添加所有方法）**

完整实现包括：read_u16_le、read_u64_le、read_f64_le、read_u8、read_bytes、skip、position、remaining、has_remaining 等

- [ ] **Step 6: 运行所有测试**

Run: `cargo test --test test_buffer_reader`
Expected: 全部 PASS

- [ ] **Step 7: 提交**

```bash
git add src/io/buffer_reader.rs tests/io/test_buffer_reader.rs
git commit -m "feat(io): implement BufferReader with zero-copy Bytes"
```

---

## Task 2.2: BufferWriter

**Files:**
- Create: `src/io/buffer_writer.rs`
- Test: `tests/io/test_buffer_writer.rs`

- [ ] **Step 1: 创建测试文件**

```rust
use rxlsb::io::BufferWriter;

#[test]
fn test_write_u32_le() {
    let mut writer = BufferWriter::new(16);
    writer.write_u32_le(0x12345678);
    let bytes = writer.freeze();
    assert_eq!(bytes.as_ref(), &[0x78, 0x56, 0x34, 0x12]);
}
```

- [ ] **Step 2: 运行测试验证失败**

- [ ] **Step 3: 实现 src/io/buffer_writer.rs**

```rust
use bytes::{BytesMut, BufMut, Bytes};

pub struct BufferWriter {
    buffer: BytesMut,
}

impl BufferWriter {
    pub fn new(capacity: usize) -> Self {
        Self { buffer: BytesMut::with_capacity(capacity) }
    }
    
    pub fn write_u32_le(&mut self, v: u32) {
        self.buffer.put_u32_le(v);
    }
    
    pub fn write_u16_le(&mut self, v: u16) { self.buffer.put_u16_le(v); }
    pub fn write_u64_le(&mut self, v: u64) { self.buffer.put_u64_le(v); }
    pub fn write_f64_le(&mut self, v: f64) { self.buffer.put_f64_le(v); }
    pub fn write_u8(&mut self, v: u8) { self.buffer.put_u8(v); }
    
    pub fn write_bytes(&mut self, bytes: &[u8]) {
        self.buffer.put_slice(bytes);
    }
    
    pub fn len(&self) -> usize { self.buffer.len() }
    pub fn capacity(&self) -> usize { self.buffer.capacity() }
    pub fn ensure_capacity(&mut self, additional: usize) {
        self.buffer.reserve(additional);
    }
    pub fn freeze(self) -> Bytes { self.buffer.freeze() }
}
```

- [ ] **Step 4: 运行测试验证通过**

- [ ] **Step 5: 提交**

```bash
git add src/io/buffer_writer.rs tests/io/test_buffer_writer.rs
git commit -m "feat(io): implement BufferWriter with BytesMut"
```

---

## Task 2.3: VarInt编解码

**Files:**
- Create: `src/io/varint.rs`
- Test: `tests/io/test_varint.rs`

- [ ] **Step 1: 创建测试文件**

```rust
use rxlsb::io::BufferWriter;
use rxlsb::io::BufferReader;
use bytes::Bytes;

#[test]
fn test_varint_encode_decode() {
    let mut writer = BufferWriter::new(8);
    let written = writer.write_varint(0x1234);
    let bytes = writer.freeze();
    
    let mut reader = BufferReader::new(bytes);
    let value = reader.read_varint().unwrap();
    assert_eq!(value, 0x1234);
}

#[test]
fn test_varint_size() {
    assert_eq!(BufferWriter::varint_size(0x00), 1);
    assert_eq!(BufferWriter::varint_size(0x80), 2);
    assert_eq!(BufferWriter::varint_size(0x4000), 3);
}
```

- [ ] **Step 2: 实现 src/io/varint.rs（扩展 BufferWriter/BufferReader）**

VarInt编码规则：
- 每字节最高位表示是否继续
- 低7位存储数据
- 小端序

```rust
// 在 BufferWriter 中添加：
impl BufferWriter {
    pub fn write_varint(&mut self, value: u32) -> usize {
        let mut written = 0;
        let mut v = value;
        loop {
            let byte = (v & 0x7F) as u8;
            v >>= 7;
            if v != 0 {
                self.write_u8(byte | 0x80);
                written += 1;
            } else {
                self.write_u8(byte);
                written += 1;
                break;
            }
        }
        written
    }
    
    pub fn varint_size(value: u32) -> usize {
        if value < 0x80 { 1 }
        else if value < 0x4000 { 2 }
        else if value < 0x200000 { 3 }
        else if value < 0x10000000 { 4 }
        else { 5 }
    }
}

// 在 BufferReader 中添加：
impl BufferReader {
    pub fn read_varint(&mut self) -> Result<u32> {
        let mut result = 0u32;
        let mut shift = 0;
        loop {
            let byte = self.read_u8()?;
            result |= ((byte & 0x7F) as u32) << shift;
            if byte & 0x80 == 0 { break; }
            shift += 7;
            if shift >= 32 { return Err(XlsbError::InvalidVarInt); }
        }
        Ok(result)
    }
}
```

- [ ] **Step 3: 运行测试验证通过**

- [ ] **Step 4: 提交**

```bash
git add src/io/buffer_reader.rs src/io/buffer_writer.rs tests/io/test_varint.rs
git commit -m "feat(io): add VarInt encode/decode for BIFF12"
```

---

## Task 2.4: UTF-16LE处理

**Files:**
- Create: `src/io/utf16.rs`
- Test: `tests/io/test_utf16.rs`

- [ ] **Step 1: 创建测试文件**

```rust
use rxlsb::io::BufferWriter;
use rxlsb::io::BufferReader;

#[test]
fn test_utf16_encode_decode() {
    let mut writer = BufferWriter::new(32);
    writer.write_wide_string("Hello");
    let bytes = writer.freeze();
    
    let mut reader = BufferReader::new(bytes);
    let text = reader.read_wide_string().unwrap();
    assert_eq!(text, "Hello");
}
```

- [ ] **Step 2: 实现 src/io/utf16.rs（扩展 BufferWriter/BufferReader）**

```rust
// 在 BufferWriter 中添加：
impl BufferWriter {
    pub fn write_wide_string(&mut self, s: &str) -> usize {
        let utf16_chars: Vec<u16> = s.encode_utf16().collect();
        let char_count = utf16_chars.len() as u32;
        
        self.write_varint(char_count);
        
        for ch in utf16_chars {
            self.write_u16_le(ch);
        }
        
        self.varint_size(char_count) + char_count as usize * 2
    }
    
    pub fn utf16le_byte_length(s: &str) -> usize {
        s.encode_utf16().count() * 2
    }
}

// 在 BufferReader 中添加：
impl BufferReader {
    pub fn read_wide_string(&mut self) -> Result<String> {
        let char_count = self.read_varint()? as usize;
        let byte_count = char_count * 2;
        
        let bytes = self.read_bytes(byte_count)?;
        
        let chars: Vec<u16> = bytes.chunks_exact(2)
            .map(|chunk| u16::from_le_bytes([chunk[0], chunk[1]]))
            .collect();
        
        String::from_utf16(&chars)
            .map_err(|_| XlsbError::InvalidUtf16)
    }
}
```

- [ ] **Step 3: 运行测试验证通过**

- [ ] **Step 4: 提交**

```bash
git add src/io/buffer_reader.rs src/io/buffer_writer.rs tests/io/test_utf16.rs
git commit -m "feat(io): add UTF-16LE encode/decode for BIFF12 strings"
```

---

## Task 2.5: 更新模块导出

**Files:**
- Modify: `src/io/mod.rs`

- [ ] **Step 1: 更新 src/io/mod.rs**

```rust
pub mod buffer_reader;
pub mod buffer_writer;
pub mod varint;
pub mod utf16;

pub use buffer_reader::BufferReader;
pub use buffer_writer::BufferWriter;
```

- [ ] **Step 2: 运行 cargo check**

Run: `cargo check`
Expected: PASS

- [ ] **Step 3: 提交**

```bash
git add src/io/mod.rs
git commit -m "feat(io): export IO layer modules"
```

---

## Task 2.6: IO层完整测试

- [ ] **Step 1: 运行所有IO层测试**

Run: `cargo test tests/io/`
Expected: 全部 PASS

- [ ] **Step 2: 提交**

```bash
git commit -m "test(io): complete IO layer tests"
```

---

**IO层计划完成。**
# rxlsb API层详细计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现用户友好的 API 层，包括 CellData、XlsbReader、XlsbWriter、TemplateFiller

**Architecture:** Builder模式 + 闭包回调 + Result错误处理

**Tech Stack:** Rust std / chrono

---

## Phase 8: CellData与数据结构

### Task 8.1: CellData enum

**Files:**
- Create: `src/api/cell_data.rs`

- [ ] **Step 1: 实现单元格数据枚举**

```rust
use chrono::{DateTime, Utc};

pub enum CellData {
    Text(String),
    Number(f64),
    Date(DateTime<Utc>),
    Bool(bool),
    Blank,
    Error(CellError),
}

pub enum CellError {
    Div0, Value, Ref, Name, Num, Na,
}

impl CellData {
    pub fn text(s: impl Into<String>) -> Self { CellData::Text(s.into()) }
    pub fn number(n: f64) -> Self { CellData::Number(n) }
    pub fn date(d: DateTime<Utc>) -> Self { CellData::Date(d) }
    pub fn bool(b: bool) -> Self { CellData::Bool(b) }
    pub fn blank() -> Self { CellData::Blank }
}
```

- [ ] **Step 2: 实现 CellSupplier trait**

```rust
pub trait CellSupplier {
    fn get_cell(&self, row: usize, col: usize) -> CellData;
}

impl<F: Fn(usize, usize) -> CellData> CellSupplier for F {
    fn get_cell(&self, row: usize, col: usize) -> CellData { self(row, col) }
}
```

- [ ] **Step 3: 实现 RowHandler trait**

```rust
pub trait RowHandler {
    fn on_row(&mut self, row_idx: usize, cells: &[CellData]);
}

impl<F: FnMut(usize, &[CellData])> RowHandler for F {
    fn on_row(&mut self, row_idx: usize, cells: &[CellData]) { self(row_idx, cells) }
}
```

- [ ] **Step 4: 提交**

---

### Task 8.2: SheetInfo

**Files:**
- Create: `src/data/sheet_info.rs`

- [ ] **Step 1: 实现 Sheet信息结构**

```rust
pub struct SheetInfo {
    pub index: usize,
    pub name: String,
}
```

- [ ] **Step 2: 提交**

---

## Phase 9: XlsbReader与XlsbWriter

### Task 9.1: XlsbWriter

**Files:**
- Create: `src/api/writer.rs`

- [ ] **Step 1: 实现 Builder**

```rust
use std::path::PathBuf;

pub struct XlsbWriterBuilder {
    path: Option<PathBuf>,
}

impl XlsbWriterBuilder {
    pub fn path(mut self, p: impl Into<PathBuf>) -> Self {
        self.path = Some(p.into());
        self
    }
    
    pub fn build(self) -> Result<XlsbWriter> {
        let path = self.path.ok_or(XlsbError::InvalidSheetIndex(0))?;
        XlsbWriter::new(&path)
    }
}
```

- [ ] **Step 2: 实现 XlsbWriter**

```rust
pub struct XlsbWriter {
    container: XlsbContainerWriter,
    sst: SstTable,
    styles: StylesRegistry,
    workbook: WorkbookWriter,
    sheets: Vec<SheetWriter<'static>>,
}

impl XlsbWriter {
    pub fn builder() -> XlsbWriterBuilder { XlsbWriterBuilder { path: None } }
    
    pub fn write_batch(&mut self, name: &str, supplier: impl CellSupplier,
                       row_count: usize, col_count: usize) -> Result<()> {
        // 创建 SheetWriter
        // 写入数据
        // 添加到 sheets 列表
    }
    
    pub fn start_sheet(&mut self, name: &str, col_count: usize) -> Result<()> { /* 实现 */ }
    pub fn write_rows(&mut self, start_row: usize, supplier: impl CellSupplier,
                      row_count: usize) -> Result<()> { /* 实现 */ }
    pub fn end_sheet(&mut self) -> Result<()> { /* 实现 */ }
    pub fn close(&mut self) -> Result<()> {
        // serialize 所有内容
        // container.finish()
    }
}
```

- [ ] **Step 3: 提交**

---

### Task 9.2: XlsbReader

**Files:**
- Create: `src/api/reader.rs`

- [ ] **Step 1: 实现 XlsbReader**

```rust
pub struct XlsbReader {
    container: XlsbContainerReader,
    sst: Option<SstTable>,
    styles: StylesRegistry,
    workbook: WorkbookReader,
}

impl XlsbReader {
    pub fn builder() -> XlsbReaderBuilder { /* 实现 */ }
    
    pub fn get_sheet_infos(&self) -> &[SheetInfo] { &self.workbook.sheet_infos }
    
    pub fn for_each_row(&mut self, sheet_idx: usize,
                        handler: impl RowHandler) -> Result<()> {
        // 获取 sheet 数据
        // 创建 SheetReader
        // 调用 for_each_row
    }
    
    pub fn read_rows(&mut self, sheet_idx: usize,
                     start_row: usize, count: usize) -> Result<Vec<Vec<CellData>>> { /* 实现 */ }
}
```

- [ ] **Step 2: 提交**

---

### Task 9.3: TemplateFiller

**Files:**
- Create: `src/api/template.rs`

- [ ] **Step 1: 实现模板填充器**

```rust
pub struct TemplateFiller {
    template_reader: XlsbReader,
    output_writer: XlsbContainerWriter,
}

impl TemplateFiller {
    pub fn builder() -> TemplateFillerBuilder { /* 实现 */ }
    
    pub fn fill_at_marker(&mut self, sheet_idx: usize, marker: &str,
                          supplier: impl CellSupplier,
                          row_count: usize, col_count: usize) -> Result<()> {
        // 查找标记位置
        // 填充数据
    }
    
    pub fn save(&mut self) -> Result<()> { /* 实现 */ }
}
```

- [ ] **Step 2: 提交**

---

### Task 9.4: 更新模块导出

- [ ] **Step 1: 更新 src/lib.rs 导出公共API**

```rust
pub use api::{CellData, CellError, XlsbReader, XlsbWriter, TemplateFiller};
pub use data::SheetInfo;
pub use error::{XlsbError, Result};
```

- [ ] **Step 2: 运行 cargo check**

- [ ] **Step 3: 提交**

---

**API层计划完成。**